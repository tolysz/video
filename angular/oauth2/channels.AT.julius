function($scope,$http,$log, $sce, title){
 title.set( $sce.trustAsHtml("About: how are we here... <small>Some OpenSource technologies used.</small>"))

  $scope.getList = function(){
      $http.get("@{YTChannelsR}")
          .success( function(data){
              $log.debug(data);
              $scope.channels = data;
          }
      )
  }

    $scope.getList()
}