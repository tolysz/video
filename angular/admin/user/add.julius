function($scope, $state, User) {
    $scope.save = function()
    {
     User.save($scope.user).$promise.then( function(newUser){
         $scope.users.push(newUser);
         $state.go('admin.user');})
    }
}