function($scope,title,$sce,$window) {
    $scope.ngVersion = angular.version.full;
    $scope.mdVersion = $window.ngMaterial.version.full;
    title.set( $sce.trustAsHtml("About: how are we here... <small>Some OpenSource technologies used.</small>"))
//     $scope.d3Version = d3.version;
//     $scope.jQVersion = jQuery.fn.jquery;
//     $scope.bsVersion = jQuery.fn.affix.Constructor.VERSION;
}